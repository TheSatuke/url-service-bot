const Config = require("../../Configuration/Config.json");
const { joinVoiceChannel } = require("@discordjs/voice");
const client = global.client;

module.exports = (member) => {
/*    client.user.setPresence({ activity: { name: ("development by nabiz"), type: "WATCHING" }, status: "online" });
    client.user.setPresence({ activities: [{ name: "development by nabiz", type: "WATCHING" }], status: "online" });
*/
}

module.exports.config = {
    Event: "ready"
}
